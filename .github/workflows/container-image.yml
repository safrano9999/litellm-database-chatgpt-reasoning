# Source of truth: SCRIPTS/githubactions. Generated copies are overwritten.
name: LiteLLM Database ChatGPT Reasoning image
run-name: ${{ inputs.monitor_id != '' && format('{0} [monitor:{1}:{2}]', github.workflow, inputs.monitor_id, inputs.monitor_final) || github.workflow }}

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: "Container image tag"
        required: false
        default: "v1.98.0-pr31332.1"
      push_latest:
        description: "Also publish :latest"
        required: true
        type: boolean
        default: true
      monitor_id:
        description: "Optional event-monitor correlation id"
        required: false
        default: ""
        type: string
      monitor_final:
        description: "This run is the final monitored stage"
        required: true
        default: true
        type: boolean

concurrency:
  group: litellm-database-chatgpt-reasoning-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read
  packages: write
  id-token: write
  attestations: write
  artifact-metadata: write

jobs:
  build-test-publish:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      IMAGE_NAME: ghcr.io/safrano9999/litellm-database-chatgpt-reasoning
      DOCKER_BUILD_RECORD_UPLOAD: "false"
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false

      - name: Resolve tags and source hashes
        id: metadata
        shell: bash
        env:
          IMAGE_TAG: ${{ inputs.image_tag }}
          PUSH_LATEST: ${{ inputs.push_latest }}
        run: |
          set -euo pipefail
          [[ "$IMAGE_TAG" =~ ^[A-Za-z0-9_.-]+$ ]]
          containerfile_sha="$(sha256sum Containerfile | cut -d' ' -f1)"
          patch_sha="$(sha256sum patches/v1.98.0-pr31332.patch | cut -d' ' -f1)"
          source_epoch="$(git show -s --format=%ct "$GITHUB_SHA")"
          created="$(date -u -d "@$source_epoch" '+%Y-%m-%dT%H:%M:%SZ')"
          {
            echo "containerfile_sha=$containerfile_sha"
            echo "patch_sha=$patch_sha"
            echo "source_epoch=$source_epoch"
            echo "created=$created"
            echo "tags<<EOF"
            echo "${IMAGE_NAME}:${IMAGE_TAG}"
            [ "$PUSH_LATEST" != true ] || echo "${IMAGE_NAME}:latest"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0
        with:
          cache-binary: false
          keep-state: false
          cleanup: true

      - name: Log in to GHCR
        uses: docker/login-action@af1e73f918a031802d376d3c8bbc3fe56130a9b0 # v4.4.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}

      - name: Build and publish
        id: build
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        env:
          SOURCE_DATE_EPOCH: ${{ steps.metadata.outputs.source_epoch }}
        with:
          context: .
          file: ./Containerfile
          platforms: linux/amd64
          pull: true
          no-cache: true
          push: true
          tags: ${{ steps.metadata.outputs.tags }}
          labels: |
            org.opencontainers.image.title=LiteLLM Database ChatGPT Reasoning
            org.opencontainers.image.description=LiteLLM Database v1.98.0 with the focused ChatGPT Responses output recovery from PR 31332
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.version=${{ inputs.image_tag }}
            org.opencontainers.image.created=${{ steps.metadata.outputs.created }}
            org.opencontainers.image.base.name=ghcr.io/berriai/litellm-database:v1.98.0
            org.opencontainers.image.base.digest=sha256:5ead13edd4efd89f32dab349c1f19447d395affca53f3aeae00f5e6e01b8c08d
            io.github.safrano9999.containerfile.sha256=${{ steps.metadata.outputs.containerfile_sha }}
            io.github.safrano9999.backport-patch.sha256=${{ steps.metadata.outputs.patch_sha }}
            io.github.safrano9999.upstream-pr=https://github.com/BerriAI/litellm/pull/31332
            io.github.safrano9999.upstream-commit=755a8b1e26224ae13cbf244bfe1b4b6077b98b61
            io.github.safrano9999.upstream-patch.sha256=a3e0327491d7373ca08643dda6d6f38bf6365fa47d572b70d57546726ec19e7d
          provenance: mode=max
          sbom: true

      - name: Attest published image
        uses: actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8 # v4.2.2
        with:
          subject-name: ${{ env.IMAGE_NAME }}
          subject-digest: ${{ steps.build.outputs.digest }}
          push-to-registry: true

      - name: Verify patched runtime
        shell: bash
        env:
          IMAGE_TAG: ${{ inputs.image_tag }}
          EXPECTED_CONTAINERFILE_SHA: ${{ steps.metadata.outputs.containerfile_sha }}
          EXPECTED_PATCH_SHA: ${{ steps.metadata.outputs.patch_sha }}
        run: |
          set -euo pipefail
          image="${IMAGE_NAME}:${IMAGE_TAG}"
          docker pull "$image"
          docker run --rm --interactive --entrypoint python3 "$image" - <<'PY'
          import inspect
          from litellm.responses.streaming_iterator import BaseResponsesAPIStreamingIterator

          source = inspect.getsource(BaseResponsesAPIStreamingIterator)
          assert "_accumulate_streamed_output_item" in source
          assert "_streamed_text_only_items" in source
          PY
          test "$(docker inspect --format '{{ index .Config.Labels "io.github.safrano9999.containerfile.sha256" }}' "$image")" = "$EXPECTED_CONTAINERFILE_SHA"
          test "$(docker inspect --format '{{ index .Config.Labels "io.github.safrano9999.backport-patch.sha256" }}' "$image")" = "$EXPECTED_PATCH_SHA"

      - name: Publish verification summary
        shell: bash
        env:
          IMAGE_TAG: ${{ inputs.image_tag }}
          IMAGE_DIGEST: ${{ steps.build.outputs.digest }}
          CONTAINERFILE_SHA: ${{ steps.metadata.outputs.containerfile_sha }}
          PATCH_SHA: ${{ steps.metadata.outputs.patch_sha }}
        run: |
          {
            echo '## Verifiable image'
            echo
            echo "- Image: \`${IMAGE_NAME}@${IMAGE_DIGEST}\`"
            echo "- Tag: \`${IMAGE_NAME}:${IMAGE_TAG}\`"
            echo "- Source commit: \`${GITHUB_SHA}\`"
            echo "- Containerfile SHA-256: \`${CONTAINERFILE_SHA}\`"
            echo "- Backport patch SHA-256: \`${PATCH_SHA}\`"
            echo '- Base: `ghcr.io/berriai/litellm-database@sha256:5ead13edd4efd89f32dab349c1f19447d395affca53f3aeae00f5e6e01b8c08d`'
          } >> "$GITHUB_STEP_SUMMARY"
